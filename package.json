{
  "name": "instadeed",
  "version": "2.0.0",
  "description": "Instadeed Legal Drafting Platform",
  "private": true,
  "scripts": {
    "build:admin-backend": "cd admin/backend && npm ci && npx prisma generate && npm run build",
    "build:admin-frontend": "cd admin/frontend && npm ci && npm run build",
    "build:landing": "cd landing && npm ci && npm run build",
    "build": "npm run build:admin-backend && npm run build:admin-frontend && npm run build:landing"
  },
  "dependencies": {
    "@babel/core": "7.26.9"
  }
}
